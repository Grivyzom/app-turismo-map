# Force pnpm to use a flat (hoisted) node_modules layout
# This is crucial for React Native / Metro Bundler because Metro does not fully support symlinks by default,
# leading to silent build failures (exit code 1) in clean Linux Docker environments.
node-linker=hoisted
shamefully-hoist=true
